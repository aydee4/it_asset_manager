[tool.ruff]
target-version = "py312"
exclude = [
    ".git",
    ".pytest_cache",
    "__pycache__",
    "instance",
    "venv",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.bandit]
exclude_dirs = [
    ".git",
    "__pycache__",
    "instance",
    "tests",
    "venv",
]
